version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ../deskkkkk
      dockerfile: Dockerfile
    ports:
      - "3000:80"
